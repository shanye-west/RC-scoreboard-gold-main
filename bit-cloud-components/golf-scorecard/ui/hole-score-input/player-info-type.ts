/**
 * Represents the information for a single player.
 * Used to identify players and display their names.
 */
export type PlayerInfo = {
  /**
   * Unique identifier for the player.
   */
  id: string;
  /**
   * Display name of the player.
   */
  name: string;
};